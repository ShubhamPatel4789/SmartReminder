name: Build Smart Reminder APK

on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3
        with:
          gradle-version: 7.6.4

      # If you still need file generation, keep your python step here.
      # Otherwise remove it and build checked-in sources directly.

      - name: Build Debug APK
        run: ./gradlew assembleDebug --stacktrace

      - name: Build release APK (debug-signed for testing)
        run: ./gradlew assembleRelease --stacktrace

      - name: Upload release APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: SmartReminder-APK-release
          path: app/build/outputs/apk/release/app-release.apk
          if-no-files-found: error

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: SmartReminder-APK
          path: app/build/outputs/apk/debug/*.apk
          if-no-files-found: error
